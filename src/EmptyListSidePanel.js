import React from "react";
import "./empty-list-side-panel.css";

const EmptyListSidePanel = () => {
  return (
    <div className="list-side-panel-empty">
      <h1 className="list-container">
        Select a channel or user from the left-side menu.
      </h1>
    </div>
  );
};

export default EmptyListSidePanel;
